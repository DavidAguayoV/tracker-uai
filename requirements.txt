# Tracker UAI - dependencias
# Instalar con:  pip install -r requirements.txt

streamlit>=1.33
streamlit-image-coordinates>=0.1.9
# opencv-contrib-python incluye cv2 + los trackers CSRT/KCF (tracking automático
# de mayor calidad). NO instalar además opencv-python: entran en conflicto.
opencv-contrib-python>=4.9
numpy>=1.26
scipy>=1.11
plotly>=5.20
pandas>=2.1
matplotlib>=3.8        # solo para exportar figuras a PNG

# --- desarrollo / tests ---
pytest>=8.0
